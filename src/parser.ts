export interface ParseResult {
  days: number;
  hours: number;
  minutes: number;
  seconds: number;
}

export function parseInput(input: string): ParseResult {
  // todo parsing logic, shunting yard should suffice
  return {} as ParseResult;
}
